*.iml
.idea
.gradle
/local.properties
/.idea/workspace.xml
/.idea/libraries
.DS_Store
/build
./app/build
app/release
app/release_mini
/captures
app/release/app-release.apk
app/build.gradle
.cxx
app/debug/app-debug.apk
app/debug/output-metadata.json

# Android keystores and signing
keystore.properties
*.keystore
*.jks

# WorkBuddy AI working directory (agent memory, skills, local state)
.workbuddy-ai/

# Eclipse / JDT language server generated files (not hand-maintained)
.settings/
.classpath
.project

# Kotlin incremental-compilation scratch (regenerated on every build)
.kotlin/

# Output of scripts/scene-adb verify (raw logcat + instrument dumps)
build/scene-adb/

# Eclipse JDT language-server output (regenerated on every save)
bin/
